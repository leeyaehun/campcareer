# 10.4C AU Nursing minimal canonical gap closure

Date: 2026-08-02
Branch: `data/au-nursing-minimal-gap-closure-20260802`

## Scope

This task produced a candidate-only package for the three existing Australia Nursing compare products. The read-only audit used the private `api_private.au_nursing_programme_catalog_v1`, `api_private.au_nursing_programme_fees_v1`, `api_private.au_nursing_programme_requirements_v1`, and `api_private.au_nursing_programme_accreditations_v1` views, plus their evidence snapshot records. Queries were explicit `SELECT` statements; no production writes, migrations, schema changes, or runtime imports were performed.

## Resolution

All three product IDs resolve exactly to a canonical programme and an international offering. The package records the existing UUIDs and CRICOS identifiers; it does not generate UUIDs or apply fuzzy matching. Dated 2026 international tuition observations with verified evidence were selected over undated legacy candidates.

## Evidence and gaps

Provider source pages and value-level snapshots are linked for identity, duration, current annual tuition, and entry requirements. Accreditation rows are intentionally not promoted: all three rows have `evidence_id = null` and `review_status = review_required`. Professional outcome, mandatory study costs, and estimated total tuition remain null rather than being inferred.

## Release decision

Status **B — Partial Candidate Package**. The package is not eligible to replace the existing UI fixture until the recorded gaps are closed. The Programs Compare UI, fixture data, URL contract, and all database objects remain unchanged.
